{
  "buildCommand": "corepack pnpm -r --filter \"./lib/**\" --if-present run build && corepack pnpm --filter @workspace/api-server run build && mkdir -p api && cp apps/api-server/dist/index.mjs api/index.mjs && cd artifacts/matric-blog && PORT=5000 BASE_PATH=/ corepack pnpm run build",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "outputDirectory": "artifacts/matric-blog/dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.mjs"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.mjs": {
      "maxDuration": 60
    }
  },
  "typescript": {
    "ignoreBuildErrors": true
  }

}
